[project]
name = "logqbit"
dynamic = ["version"]
description = "Simple data logger and live plotter for lab-scale experiments."
readme = "README.md"
requires-python = ">=3.11"
authors = [{ name = "Qiujv", email = "qiujv@outlook.com" }]
dependencies = [
    "numpy>=2.3.3",
    "pandas>=2.3.3",
    "pyarrow>=21.0.0",
    "ruamel-yaml>=0.18.15",
    "tqdm>=4.67.1",
    "typing-extensions>=4.15.0",
]

[dependency-groups]
dev = [
    "ipykernel>=6.30.1",
    "ipywidgets>=8.1.7",
    "matplotlib>=3.10.7",
    "mkdocs>=1.6.1",
    "mkdocstrings[python]>=0.29.1",
    "pytest>=8.4.2",
    "pytest-cov>=7.0.0",
    "ruff>=0.15.12",
]

[project.optional-dependencies]
gui = [
    "numba>=0.65.0",
    "pygments>=2.19.2",
    "pyqtgraph>=0.14.0",
    "pyside6>=6.9.3",
    "scipy>=1.16.0",
    "send2trash>=1.8.3",
]

[project.urls]
Homepage = "https://github.com/Qiujv/logqbit"

[project.scripts]
logqbit = "logqbit.cli:main"

[project.gui-scripts]
logqbit-live-plotter = "logqbit.gui.live_plotter:main"
logqbit-browser = "logqbit.gui.browser.startup:main"

[build-system]
requires = ["hatchling", "hatch-vcs"]
build-backend = "hatchling.build"

[tool.hatch.version]
source = "vcs"

[tool.hatch.build.targets.wheel]
packages = ["src/logqbit"]

[tool.hatch.build.targets.sdist]
include = ["src/", "tests/", "README.md", "LICENSE", "pyproject.toml"]

[tool.pytest.ini_options]
testpaths = ["tests"]

[tool.ruff.format]
exclude = ["notebooks/**"]
